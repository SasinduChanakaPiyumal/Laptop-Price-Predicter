#
# This file is intended to be autogenerated by pip-compile with Python 3.11
# by the following command:
#
#    pip-compile requirements.in --generate-hashes --output-file=requirements.txt
#
# NOTE: Run the following commands to generate actual pinned dependencies with hashes:
#
#   pip install pip-tools
#   pip-compile requirements.in --generate-hashes -o requirements.txt
#   pip-sync requirements.txt
#
# The entries below mirror requirements.in and will be fully pinned with all
# transitive dependencies when pip-compile is executed.

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0
pytest>=7.0.0
pytest-cov>=4.0.0

# After running pip-compile with --generate-hashes, this file will contain
# all resolved versions along with SHA256 hashes for each package.
